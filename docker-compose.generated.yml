services:
  nexus-sim:
    build:
      context: .
      dockerfile: docker/Dockerfile.webots
    image: nexus/webots:R2023b
    container_name: nexus_webots
    environment:
      - DISPLAY=${DISPLAY}
      - WEBOTS_HOME=/usr/local/webots
      - LIBGL_ALWAYS_SOFTWARE=1
      - GALLIUM_DRIVER=llvmpipe
      - XAUTHORITY=/root/.Xauthority
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ${HOME}/.Xauthority:/root/.Xauthority:ro
      - ./simulators/webots/worlds:/nexus/worlds
      - .:/nexus
    networks:
      - nexus_net
    command: bash -c "webots /nexus/worlds/simple_road.wbt"
  nexus-ros2:
    build:
      context: .
      dockerfile: docker/Dockerfile.ros2
    image: nexus/ros2:humble
    container_name: nexus_ros2
    environment:
      - DISPLAY=${DISPLAY}
      - ROS_DOMAIN_ID=0
      - PYTHONPATH=/nexus
      - WEBOTS_SERVER=nexus_webots:1234
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      
      - .:/nexus
    working_dir: /nexus
    depends_on:
      - nexus-sim
    networks:
      - nexus_net
    command: bash -c "source /opt/ros/humble/setup.bash && echo '✅ Nexus ROS2 container ready' && sleep 5 && python3 /nexus/simulators/webots/extern_controller.py & sleep 3 && python3 -m nexus.cli.main run --config /nexus/projects/webots_lidar_demo/nexus.yaml"

networks:
  nexus_net:
    driver: bridge
